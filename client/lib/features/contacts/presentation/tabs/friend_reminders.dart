import 'package:flutter/material.dart';

class FriendReminders extends StatelessWidget {
  const FriendReminders({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Friend Reminders Screen');
  }
}
